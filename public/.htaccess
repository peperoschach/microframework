<IfModule mod_rewite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} -s [OR]
    RewriteCond %{REQUEST_FILENAME} -l [OR]
    RewriteCond %{REQUEST_FILENAME} -d [OR]
    RewriteCond ^.*$ - [NC,L]
    RewriteCond ^.*$ index.php [NC,L]
</IfModule>